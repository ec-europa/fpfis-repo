sudo: required
dist: trusty
language: generic
services:
  - docker
env:
  matrix:
    - EL=6
    - EL=7
  global:
    - ./build-docker ${EL} ${TRAVIS_BRANCH}
after_failure:
  - tail -n 300 SRPMS/*log
  - tail -n 300 RPMS/*log
