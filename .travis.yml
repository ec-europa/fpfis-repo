sudo: required
dist: trusty
language: generic
services:
  - docker
env:
  matrix:
    - EL=6
    - EL=7
script:
    - ./build-docker.sh ${EL} ${TRAVIS_BRANCH}
after_failure:
  - tail -n 300 SRPMS/*log
  - tail -n 300 RPMS/*log
